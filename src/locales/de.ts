import en, { type LocaleKeys } from './en';

const de: Record<LocaleKeys, string> = {
  ...en,
  'profile.title': 'Profil',
  'profile.language': 'Sprache',
  'profile.select_language': 'Sprache waehlen',
};

export default de;
